# 本地开发配置模板：复制为 .env 后按需修改（真实环境变量优先于此文件；勿把生产密钥提交进版本库）
# 必填两项，长度/安全性见 src/adapters/crypto/webcrypto.ts（MASTER_KEY 至少 16 字符）
MASTER_KEY=dev-master-key-0123456789
ADMIN_PASSWORD=dev-admin

# Node/Docker 本地服务端口（默认 8787）
# PORT=8787

# SQLite 数据库文件路径（默认 ./data/gateway.db）
# DATABASE_PATH=./data/gateway.db

# 前端构建产物目录（存在则托管管理后台 SPA；默认 ./frontend/dist）
# FRONTEND_DIST=./frontend/dist

# 每个网关 key 每小时邮箱创建请求的默认上限（0 = 不限，默认 60；后台可为单个 key 覆盖）
# MAILBOXES_PER_KEY_PER_HOUR=60

# 每个 Key 默认并发请求上限（统一 API 与透传合计；0 = 不限，兼容原有行为）
# MAX_CONCURRENT_REQUESTS_PER_KEY=0
# 上述默认值及验活设置可在后台“全局设置”保存；数据库中的值优先于环境变量。

# 健康监控全局默认测活间隔（毫秒，默认 300000 = 5 分钟，下限 60000）
# HEALTH_CHECK_INTERVAL_MS=300000

# 跨域管理后台来源（逗号分隔多个，或 * 供非 cookie 客户端调试）。
# 注意：配置为 * 时不会回显 Allow-Credentials（会话 cookie 保持 SameSite=Lax，
# 防任意站点驱动管理 API）；跨域管理后台必须写明确切来源。
# ADMIN_CORS_ORIGIN=http://localhost:5173
